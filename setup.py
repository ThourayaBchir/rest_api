import setuptools
from setuptools import setup


setup(
    name='rest_api',
    version='1.0',
    package_dir={"rest_api": "rest_api"},
    packages=setuptools.find_packages(),
    url='',
    author='thouraya',
)
